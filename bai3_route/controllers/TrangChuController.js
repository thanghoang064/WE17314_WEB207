window.TrangChuController = function($scope,$routeParams) {
    $scope.title = "Đây là trang giới thiệu";
    // lấy id khi ấn nút onedit bắn sang 
    let id = routeParams.id;
    // xử lý toàn bộ chức năng sửa ở bên controller này 
    
}